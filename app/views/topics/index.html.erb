<div class="row">
	<div class="span12 text-center">
		<h1>All Clonemarks</h1>
	</div>
</div>
<% @topics.each do |topic| %>
	<div class="row">
	<%= link_to topic.name, topic %>
		<div class="row"
		<% topic.bookmarks.each do |bookmark| %>
			<div class="span2">
			<%= link_to bookmark.name, bookmark %>
			</div>
		<% end %>
		<div>
	</div>
<% end %>